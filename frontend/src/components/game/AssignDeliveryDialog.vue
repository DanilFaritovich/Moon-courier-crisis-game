<script setup lang="ts">
import type { MapPoint, Order, Rover } from '../../types/game'
defineProps<{ rover: Rover; order: Order; destination?: MapPoint; distance?: number; busy: boolean }>()
defineEmits<{ confirm: []; close: [] }>()
</script>
<template><div class="dialog-backdrop"><section class="delivery-dialog" role="dialog" aria-modal="true" aria-labelledby="delivery-title"><span class="eyebrow">CONFIRM DEPLOYMENT</span><h2 id="delivery-title">Assign {{ rover.name }}</h2><p>Send rover #{{ rover.id }} to <b>{{ destination?.name ?? `point ${order.destination_point_id}` }}</b> for contract #{{ order.id }}.</p><dl><div><dt>PAYLOAD</dt><dd>{{ order.weight }} kg</dd></div><div><dt>REWARD</dt><dd>¢{{ order.reward }}</dd></div><div><dt>DISTANCE</dt><dd>{{ distance ? `${distance} km direct route` : 'Route distance unavailable' }}</dd></div><div><dt>BATTERY</dt><dd>{{ rover.battery }}%</dd></div></dl><p class="dialog-note">Battery use and route planning are not exposed by the current API.</p><footer><button class="quiet-button" :disabled="busy" @click="$emit('close')">CANCEL</button><button class="confirm-button" :disabled="busy" @click="$emit('confirm')">{{ busy ? 'ASSIGNING…' : 'CONFIRM DELIVERY' }}</button></footer></section></div></template>
